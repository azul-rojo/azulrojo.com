import survivalKits from '../../md/survivalkits.md';
import { PageMarkdown } from "../../component/PageMarkdown";

const SurvivalKits = () => {
  return <PageMarkdown mdSource={survivalKits} />
};

export default SurvivalKits;
